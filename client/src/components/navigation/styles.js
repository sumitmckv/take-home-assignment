const { makeStyles } = require("@material-ui/core");

const navbarStyles = makeStyles({
  title: {
    flexGrow: 1,
  },
  btn: {
    textTransform: "none",
    fontSize: 16,
  },
});

export default navbarStyles;
